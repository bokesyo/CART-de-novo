已经完成的工作：
文件夹：
classref 里面是彭兄写的树class 和 stack class（俊华写的）还有一个存储变量的localcache class（网上找的）
dataStorge 是一个存储中间变量的文件夹
inputdata 是包含 训练数据的文件夹
output 是存储输出树的图像文件夹


primartGrow.py
输入：训练数据
输出1：在dataStorge里面输出一个db文件，存储的是决策树的根节点
输出2：在output里面输出一个python脚本，是决策树的执行程序，往里面输入一瓶红酒的全部数据，他就给出一个分数。可以试试看，一共输入11个指标，组成一个列表。

printTree.py
输入：dataStorge里面输出一个db文件，决策树的根节点
输出：一幅树的图像 在output文件夹里

regCalcFunc.py
就是算方差的函数，以及一些杂乱功能。不需要注意。